<UserControl x:Class="OCC.WpfClient.Features.ProcurementHub.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.ProcurementHub.ViewModels"
             xmlns:ap="clr-namespace:OCC.WpfClient.Infrastructure.AttachedProperties"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1024"
             d:DataContext="{d:DesignInstance Type=vm:InventoryViewModel}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid Margin="5,10,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header & Search -->
            <Grid Grid.Row="0" Margin="0,0,0,25">
                <StackPanel>
                    <TextBlock Text="Inventory Management" FontSize="32" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Track stock levels, reorder points, and warehouse locations." 
                               FontSize="14" Foreground="{StaticResource TextSub}" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="{StaticResource InputBackground}" CornerRadius="20" Width="300" Height="40" Padding="15,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path Data="{StaticResource IconSearch}" Fill="{StaticResource TextSub}" 
                                  Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" Opacity="0.6"/>
                            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="Transparent" BorderThickness="0" Foreground="White" 
                                     VerticalAlignment="Center" Margin="10,0,0,0" ap:WatermarkProperties.Watermark="Search items or SKUs..."/>
                        </Grid>
                    </Border>
                    <Button Content="Refresh" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" 
                            Margin="15,0,0,0" Width="100" Height="40"/>
                </StackPanel>
            </Grid>

            <!-- DataGrid -->
            <Border Grid.Row="1" Style="{StaticResource GlassCard}" Padding="0">
                <Grid>
                    <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" 
                              Background="Transparent" BorderThickness="0" CanUserAddRows="False"
                              IsReadOnly="True" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="120"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                            <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityOnHand}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource ErrorRed}"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="150"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty State -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                                Visibility="{Binding Items.Count, Converter={StaticResource VisibilityConverter}, ConverterParameter=0}">
                        <Path Data="{StaticResource IconPortfolio}" Fill="{StaticResource TextSub}" 
                              Opacity="0.2" Width="60" Height="60" Stretch="Uniform"/>
                        <TextBlock Text="No items found." Margin="0,15" Foreground="{StaticResource TextSub}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Busy Overlay -->
        <Grid Visibility="{Binding IsBusy, Converter={StaticResource VisibilityConverter}}" Background="#33000000">
             <ProgressBar IsIndeterminate="True" Width="200" Height="4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
